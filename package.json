{
  "name": "network-viewer",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n bridge,web -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start:bridge": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
